# Required to run live OpenAI-powered board members.
# Create .env.local from this file and add your own key. Never commit .env.local.
OPENAI_API_KEY=

# Required only for local fal.ai image-to-video generation.
# Create an API-scoped key at https://fal.ai/dashboard/keys and keep it server-side.
FAL_KEY=

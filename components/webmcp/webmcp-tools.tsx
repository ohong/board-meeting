"use client";

/**
 * WebMCP tool registration — OWNED BY THE WEBMCP WORKSTREAM.
 *
 * Registers the six site tools (lib/meeting/types.ts WEBMCP_TOOL_NAMES) on
 * document.modelContext and routes them to MeetingSession's shared actions.
 *
 * TODO(webmcp): implement. This stub renders nothing.
 */
export function WebMCPTools() {
  return null;
}
